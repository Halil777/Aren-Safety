# App
NODE_ENV=development
PORT=3000

# Disable DB to run without Postgres (uses file storage for tenants)
DB_DISABLED=true

# Database connection (for app + CLI)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=QwertyWeb123321
DB_NAME=safety
DB_SCHEMA=public
DB_SSL=false

# Docker-only (compose will use these for Postgres service)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=safety
